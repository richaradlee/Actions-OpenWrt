name: ImmortalWrt x86
on:
  schedule:
    - cron: "0 10,18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    if: github.event_name == 'schedule' || github.event.repository.owner.id == github.event.sender.id    
    permissions:
      contents: write
     # actions: write

    name: ImmortalWrt 编译
    steps:
      # 1. 优化构建空间
      - name: 优化构建空间
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 4096
          temp-reserve-mb: 1024
          swap-size-mb: 8192
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      # 2. 检出 workflow 仓库
      - name: 检出代码
        uses: actions/checkout@v4

      # 3. 安装编译依赖
      - name: 安装编译依赖
        env:
          DEBIAN_FRONTEND: noninteractive
          TZ: Asia/Shanghai
        run: |
          set -e
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup /usr/lib/jvm /etc/mysql /etc/php || true
          sudo -E apt-get update
          sudo -E apt-get install -y ack asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gettext git gperf help2man intltool libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm make ninja-build patch pkgconf python3 python3-pyelftools python3-setuptools rsync scons squashfs-tools subversion swig texinfo uglifyjs unzip wget xmlto xxd zlib1g-dev scdoc
          sudo -E timedatectl set-timezone "Asia/Shanghai"

      # 4. 克隆 ImmortalWrt 25.12 源码
      - name: 克隆 ImmortalWrt 源码
        env:
          REPO_URL: https://github.com/immortalwrt/immortalwrt
          REPO_BRANCH: openwrt-25.12
        run: |
          set -e
          git clone --depth 1 "$REPO_URL" -b "$REPO_BRANCH" openwrt
          cd openwrt
          echo "============================================================"
          echo "IMMORTALWRT SOURCE"
          echo "============================================================"
          git branch --show-current
          git log -1 --format='commit: %H'
          git log -1 --format='subject: %s'
          echo "SOURCE_COMMIT=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

      # 5. 配置 feeds
      - name: 配置上游 feeds
        working-directory: ./openwrt
        run: |
          set -e
          sed -i '1i src-git rtp2httpd https://github.com/stackia/rtp2httpd.git' feeds.conf.default
          cat feeds.conf.default

      # 6. 更新并安装官方 feeds
      - name: 更新并安装官方 feeds
        working-directory: ./openwrt
        run: |
          set -e
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # 7. 安装第三方软件包
      - name: 安装第三方软件包
        working-directory: ./openwrt
        run: |
          set -e

          rm -rf package/feeds/luci/luci-app-openclash package/openclash package/feeds/luci/openclash
          git clone --depth 1 https://github.com/vernesong/OpenClash.git package/openclash

          rm -rf package/feeds/luci/luci-app-passwall2 package/passwall2
          git clone --depth 1 https://github.com/Openwrt-Passwall/openwrt-passwall2.git package/passwall2

          rm -rf package/dae package/feeds/base/dae
          git clone --depth 1 https://github.com/QiuSimons/luci-app-daed.git package/dae

          rm -rf package/feeds/lucky/luci-app-lucky package/lucky
          git clone --depth 1 https://github.com/gdy666/luci-app-lucky.git package/lucky

          rm -rf package/feeds/luci/luci-theme-argon package/feeds/luci/luci-app-argon-config package/argon package/argon-config
          git clone --depth 1 --branch master https://github.com/jerrykuku/luci-theme-argon.git package/feeds/luci/luci-theme-argon
          git clone --depth 1 --branch master https://github.com/jerrykuku/luci-app-argon-config.git package/feeds/luci/luci-app-argon-config

      # 8. 验证第三方源码
      - name: 验证第三方软件包源码
        working-directory: ./openwrt
        run: |
          set -e

          check_makefile() {
            NAME="$1"
            PATH_TO_MAKEFILE="$2"
            echo ">>> Checking $NAME"
            echo "Path: $PATH_TO_MAKEFILE"
            test -f "$PATH_TO_MAKEFILE" || {
              echo "ERROR: Makefile not found."
              exit 1
            }
            echo "OK: Makefile exists."
          }

          check_makefile "OpenClash" package/openclash/luci-app-openclash/Makefile
          check_makefile "PassWall2" package/passwall2/luci-app-passwall2/Makefile
          check_makefile "DAED" package/dae/luci-app-daed/Makefile
          check_makefile "Lucky" package/lucky/luci-app-lucky/Makefile
          check_makefile "Argon" package/feeds/luci/luci-theme-argon/Makefile
          check_makefile "Argon Config" package/feeds/luci/luci-app-argon-config/Makefile

          ARGON_REMOTE="$(git -C package/feeds/luci/luci-theme-argon remote get-url origin)"
          ARGON_BRANCH="$(git -C package/feeds/luci/luci-theme-argon branch --show-current)"
          ARGON_COMMIT="$(git -C package/feeds/luci/luci-theme-argon rev-parse HEAD)"
          ARGON_VERSION="$(git -C package/feeds/luci/luci-theme-argon describe --tags --always 2>/dev/null || echo unknown)"

          echo "============================================================"
          echo "ARGON SOURCE"
          echo "============================================================"
          echo "Remote: $ARGON_REMOTE"
          echo "Branch: $ARGON_BRANCH"
          echo "Commit: $ARGON_COMMIT"
          echo "Version: $ARGON_VERSION"

          test "$ARGON_REMOTE" = "https://github.com/jerrykuku/luci-theme-argon.git"
          test "$ARGON_BRANCH" = "master"
          grep -q "^PKG_VERSION:=" package/feeds/luci/luci-theme-argon/Makefile

          echo "ARGON_COMMIT=$ARGON_COMMIT" >> "$GITHUB_ENV"
          echo "ARGON_VERSION=$ARGON_VERSION" >> "$GITHUB_ENV"

          echo "============================================================"
          echo "THIRD-PARTY SOURCE CHECK PASSED"
          echo "============================================================"

      # 9. 自定义应用配置
      - name: 自定义应用配置
        working-directory: ./openwrt
        run: |
          set -e
          rm -f .config .config.old
          touch .config

          cat >> .config <<'EOF'
          # ============================================================
          # Target / Image / Build
          # ============================================================
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          CONFIG_TARGET_KERNEL_PARTSIZE=128
          CONFIG_TARGET_ROOTFS_PARTSIZE=640
          CONFIG_TARGET_IMAGES_GZIP=y
          CONFIG_EFI_IMAGES=y
          CONFIG_CCACHE=y
          # ============================================================
          # IPv6 / IPv6 Firewall
          # ============================================================
          CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_odhcp6c=y
          CONFIG_PACKAGE_odhcpd-ipv6only=y
          CONFIG_PACKAGE_luci-proto-ipv6=y
          CONFIG_PACKAGE_ip6tables=y
          CONFIG_PACKAGE_kmod-nft-nat6=y
          # ============================================================
          # Network / Interface
          # ============================================================
          CONFIG_PACKAGE_kmod-bonding=y
          CONFIG_PACKAGE_proto-bonding=y
          CONFIG_PACKAGE_luci-proto-bonding=y
          # ============================================================
          # LuCI Core / Web Management
          # ============================================================
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_ttyd=y
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-diskman=y
          CONFIG_PACKAGE_luci-app-irqbalance=y
          CONFIG_PACKAGE_luci-app-package-manager=y
          # ============================================================
          # SQM / QoS / CAKE
          # ============================================================
          CONFIG_PACKAGE_luci-app-sqm=y
          CONFIG_PACKAGE_sqm-scripts=y
          CONFIG_PACKAGE_kmod-sched-cake=y
          CONFIG_PACKAGE_kmod-ifb=y
          CONFIG_PACKAGE_tc-full=y
          # ============================================================
          # Streaming / IPTV
          # ============================================================
          CONFIG_PACKAGE_luci-app-rtp2httpd=y
          # ============================================================
          # Proxy / Traffic Routing
          # ============================================================
          CONFIG_PACKAGE_luci-app-passwall2=y
          CONFIG_PACKAGE_luci-app-openclash=y
          # ============================================================
          # DAED / BPF / XDP
          # ============================================================
          CONFIG_DEVEL=y
          CONFIG_KERNEL_DEBUG_INFO=y
          CONFIG_KERNEL_DEBUG_INFO_BTF=y
          CONFIG_KERNEL_CGROUPS=y
          CONFIG_KERNEL_CGROUP_BPF=y
          CONFIG_KERNEL_BPF_EVENTS=y
          CONFIG_BPF_TOOLCHAIN_HOST=y
          CONFIG_KERNEL_XDP_SOCKETS=y
          CONFIG_PACKAGE_kmod-xdp-sockets-diag=y
          CONFIG_PACKAGE_daed=y
          CONFIG_PACKAGE_luci-app-daed=y
          CONFIG_PACKAGE_kmod-nft-tproxy=y
          CONFIG_PACKAGE_kmod-nft-socket=y
          CONFIG_PACKAGE_kmod-nft-compat=y
          CONFIG_PACKAGE_nftables=y
          CONFIG_PACKAGE_ip-full=y
          # ============================================================
          # OpenVPN
          # ============================================================
          CONFIG_PACKAGE_luci-app-openvpn=y
          CONFIG_PACKAGE_openvpn-easy-rsa=y
          CONFIG_PACKAGE_openvpn-openssl=y
          # ============================================================
          # WireGuard
          # ============================================================
          CONFIG_PACKAGE_wireguard-tools=y
          CONFIG_PACKAGE_kmod-wireguard=y
          CONFIG_PACKAGE_luci-proto-wireguard=y
          # ============================================================
          # Lucky
          # ============================================================
          CONFIG_PACKAGE_luci-app-lucky=y
          # ============================================================
          # Argon Theme
          # ============================================================
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_PACKAGE_luci-app-argon-config=y
          # ============================================================
          # UPnP / NAT-PMP / PCP
          # ============================================================
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_miniupnpd-nftables=y
          CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y
          # ============================================================
          # Vlmcsd / KMS
          # ============================================================
          CONFIG_PACKAGE_vlmcsd=y
          CONFIG_PACKAGE_luci-app-vlmcsd=y
          CONFIG_PACKAGE_luci-i18n-vlmcsd-zh-cn=y
          # ============================================================
          # System / CLI Tools
          # ============================================================
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_screen=y
          CONFIG_PACKAGE_tree=y
          CONFIG_PACKAGE_wget=y
          # ============================================================
          # Network Services / Utilities
          # ============================================================
          CONFIG_PACKAGE_haproxy=y
          CONFIG_PACKAGE_socat=y
          CONFIG_SOCAT_SSL=y
          # ============================================================
          # DNS / Proxy Core
          # ============================================================
          CONFIG_SING_BOX_WITH_ECH=y
          CONFIG_PACKAGE_sing-box=y
          CONFIG_PACKAGE_chinadns-ng=y
          # ============================================================
          # Virtualization
          # ============================================================
          CONFIG_PACKAGE_open-vm-tools=y
          # ============================================================
          # Network Diagnostics / Performance
          # ============================================================
          CONFIG_PACKAGE_iperf3=y
          CONFIG_PACKAGE_iftop=y
          CONFIG_PACKAGE_tcpdump=y
          CONFIG_PACKAGE_pv=y
          CONFIG_PACKAGE_bind-host=y
          CONFIG_PACKAGE_tcping=y
          # ============================================================
          # Firewall / Network Utilities
          # ============================================================
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_dns2socks=y
          CONFIG_PACKAGE_geoview=y
          # ============================================================
          # System Performance
          # ============================================================
          CONFIG_PACKAGE_irqbalance=y

          EOF

          cp .config .config.requested

      # 10. 配置 ccache
      - name: 配置 ccache
        working-directory: ./openwrt
        run: |
          set -e
          mkdir -p .ccache
          ccache --set-config=max_size=5G
          ccache --set-config=compression=true
          ccache -z
          ccache -s

      # 11. 恢复下载缓存
      - name: 恢复下载缓存
        uses: actions/cache/restore@v4
        id: cache-dl
        with:
          path: ./openwrt/dl
          key: ${{ runner.os }}-immortalwrt-dl-v4-${{ env.SOURCE_COMMIT }}
          restore-keys: |
            ${{ runner.os }}-immortalwrt-dl-v4-
            ${{ runner.os }}-immortalwrt-dl-

      # 12. 恢复 ccache 缓存
      - name: 恢复 ccache 缓存
        uses: actions/cache/restore@v4
        id: cache-ccache
        with:
          path: ./openwrt/.ccache
          key: ${{ runner.os }}-immortalwrt-ccache-v4
          restore-keys: |
            ${{ runner.os }}-immortalwrt-ccache-

      # 13. 恢复工具链缓存
      - name: 恢复工具链缓存
        uses: actions/cache/restore@v4
        id: cache-staging
        with:
          path: |
            ./openwrt/staging_dir/host
            ./openwrt/staging_dir/toolchain-*
          key: ${{ runner.os }}-immortalwrt-staging-v4-${{ env.SOURCE_COMMIT }}-${{ hashFiles('openwrt/.config') }}
          restore-keys: |
            ${{ runner.os }}-immortalwrt-staging-v4-
            ${{ runner.os }}-immortalwrt-staging-

      # 14. 运行 make defconfig
      - name: 运行 make defconfig
        working-directory: ./openwrt
        run: |
          set -o pipefail
          make defconfig 2>&1 | tee defconfig.log

      # 15. 验证配置
      - name: 验证配置
        working-directory: ./openwrt
        run: |
          set -e
          FAILED=0

          check_y() {
            SYMBOL="$1"
            VALUE="$(grep "^${SYMBOL}=" .config 2>/dev/null | cut -d= -f2 || true)"
            echo "$SYMBOL = ${VALUE:-<NOT SET>}"
            [ "$VALUE" = y ] || {
              echo "ERROR: $SYMBOL is not enabled."
              FAILED=1
            }
          }

          check_not_y() {
            SYMBOL="$1"
            VALUE="$(grep "^${SYMBOL}=" .config 2>/dev/null | cut -d= -f2 || true)"
            echo "$SYMBOL = ${VALUE:-<NOT SET>}"
            [ "$VALUE" != y ] || {
              echo "ERROR: $SYMBOL unexpectedly enabled."
              FAILED=1
            }
          }

          echo "===== 必选组件 ====="

          for P in \
            luci \
            luci-app-openclash \
            luci-app-passwall2 \
            daed \
            luci-app-daed \
            luci-app-lucky \
            luci-app-rtp2httpd \
            luci-app-argon-config \
            luci-app-upnp \
            miniupnpd-nftables \
            luci-i18n-upnp-zh-cn \
            vlmcsd \
            luci-app-vlmcsd \
            luci-i18n-vlmcsd-zh-cn \
            luci-app-sqm \
            sqm-scripts \
            kmod-sched-cake \
            kmod-ifb \
            tc-full \
            sing-box \
            chinadns-ng \
            open-vm-tools \
            iperf3 \
            tcpdump \
            haproxy \
            socat \
            luci-app-openvpn \
            openvpn-easy-rsa \
            kmod-wireguard \
            wireguard-tools \
            luci-proto-wireguard \
            ccachedummy
          do
            if [ "$P" = "ccachedummy" ]; then
              continue
            fi
            check_y "CONFIG_PACKAGE_$P"
          done

          echo
          echo "===== Argon 主题 ====="

          test -f package/feeds/luci/luci-theme-argon/Makefile || {
            echo "ERROR: Argon theme Makefile not found."
            FAILED=1
          }

          test -f package/feeds/luci/luci-app-argon-config/Makefile || {
            echo "ERROR: Argon config Makefile not found."
            FAILED=1
          }

          grep -q "^CONFIG_PACKAGE_luci-app-argon-config=y$" .config || {
            echo "ERROR: Argon config package is not enabled."
            FAILED=1
          }

          grep -R -n "luci-theme-argon" package/feeds/luci/luci-app-argon-config/Makefile >/dev/null || {
            echo "ERROR: Argon config dependency not found."
            FAILED=1
          }

          echo "Argon commit: ${ARGON_COMMIT}"
          echo "Argon version: ${ARGON_VERSION}"

          echo
          echo "===== 已禁用组件 ====="

          check_not_y CONFIG_PACKAGE_luci-app-openvpn-server

          echo
          echo "===== 警告检查 ====="

          if grep -q "Not overriding core package 'luci-app-openclash'" defconfig.log; then
            echo "ERROR: OpenClash override warning."
            FAILED=1
          fi

          if grep -q "package/feeds/luci/luci-app-argon-config/Makefile.*depends on 'luci-theme-argon', which does not exist" defconfig.log; then
            echo "ERROR: Argon dependency warning."
            FAILED=1
          fi

          if [ "$FAILED" -ne 0 ]; then
            echo
            echo "============================================================"
            echo "最终配置验证失败"
            echo "============================================================"
            exit 1
          fi

          echo
          echo "============================================================"
          echo "最终配置验证通过"
          echo "============================================================"

      # 16. 审计重要组件
      - name: 审计重要组件
        working-directory: ./openwrt
        run: |
          set -e

          echo "===== ImmortalWrt ====="
          git log -1 --format='commit: %H'
          git log -1 --format='subject: %s'

          echo
          echo "===== Argon ====="
          echo "Commit: ${ARGON_COMMIT}"
          echo "Version: ${ARGON_VERSION}"

          echo
          echo "===== UPnP / PCP / NAT-PMP ====="
          grep -E '^CONFIG_PACKAGE_(luci-app-upnp|miniupnpd-nftables|luci-i18n-upnp-zh-cn)=' .config || true
          echo "miniupnpd 提供 UPnP IGD、NAT-PMP 和 PCP 支持。"

          echo
          echo "===== Vlmcsd ====="
          grep -E '^CONFIG_PACKAGE_(vlmcsd|luci-app-vlmcsd|luci-i18n-vlmcsd-zh-cn)=' .config || true

          echo
          echo "===== OpenVPN ====="
          grep -E '^CONFIG_PACKAGE_(luci-app-openvpn|openvpn-easy-rsa|luci-app-openvpn-server)=' .config || true

          echo
          echo "===== WireGuard ====="
          grep -E '^CONFIG_PACKAGE_(kmod-wireguard|wireguard-tools|luci-proto-wireguard)=' .config || true

          echo
          echo "===== DAED / BTF ====="
          grep -E '^CONFIG_(DEVEL|KERNEL_DEBUG_INFO|KERNEL_DEBUG_INFO_BTF|KERNEL_CGROUPS|KERNEL_CGROUP_BPF|KERNEL_BPF_EVENTS|BPF_TOOLCHAIN_HOST|KERNEL_XDP_SOCKETS)=' .config || true

          echo
          echo "===== 缓存 ====="
          echo "CCACHE_DIR=$PWD/.ccache"
          ccache -s || true

          echo
          echo "===== 依赖警告 ====="
          grep -n -E 'WARNING:|depends on|dependency|not found|unknown symbol|invalid' defconfig.log || true

      # 17. 编译固件
      - name: 编译 ImmortalWrt
        working-directory: ./openwrt
        env:
          TZ: Asia/Shanghai
          CCACHE_DIR: ${{ github.workspace }}/openwrt/.ccache
        run: |
          set -o pipefail
          export FORCE_UNSAFE_CONFIGURE=1

          mkdir -p "$CCACHE_DIR"
          ccache --set-config=max_size=5G
          ccache -z

          echo "============================================================"
          echo "编译前 CCACHE 状态"
          echo "============================================================"
          ccache -s || true

          echo "============================================================"
          echo "开始编译 IMMORTALWRT"
          echo "============================================================"

          make -j"$(nproc)" download
          make -j"$(nproc)" world V=s 2>&1 | tee build.log

          echo
          echo "============================================================"
          echo "编译后 CCACHE 状态"
          echo "============================================================"
          ccache -s || true

      # 18. 保存下载缓存
      - name: 保存下载缓存
        if: success() && steps.cache-dl.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ./openwrt/dl
          key: ${{ runner.os }}-immortalwrt-dl-v4-${{ env.SOURCE_COMMIT }}

      # 19. 保存 ccache 缓存
      - name: 保存 ccache 缓存
        if: success() && steps.cache-ccache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ./openwrt/.ccache
          key: ${{ runner.os }}-immortalwrt-ccache-v4

      # 20. 保存工具链缓存
      - name: 保存工具链缓存
        if: success() && steps.cache-staging.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ./openwrt/staging_dir/host
            ./openwrt/staging_dir/toolchain-*
          key: ${{ runner.os }}-immortalwrt-staging-v4-${{ env.SOURCE_COMMIT }}-${{ hashFiles('openwrt/.config') }}

      # 21. 收集 EFI 镜像和校验和
      - name: 收集发布文件
        working-directory: ./openwrt
        run: |
          set -e

          mkdir -p release
          DATE="$(TZ=Asia/Shanghai date +%Y%m%d-%H%M)"
          SRC="$(find bin/targets/x86/64 -maxdepth 1 -type f -name '*combined-efi.img.gz' | head -n 1)"

          if [ -z "$SRC" ]; then
            echo "ERROR: EFI image not found."
            find bin/targets/x86/64 -maxdepth 1 -type f -print
            exit 1
          fi

          FILE="ImmortalWrt-x86-64-Generic-${DATE}.img.gz"

          cp "$SRC" "release/$FILE"
          sha256sum "release/$FILE" > release/SHA256SUMS.txt

          cat > release/buildinfo.txt <<EOF
          ImmortalWrt branch: openwrt-25.12
          ImmortalWrt commit: ${SOURCE_COMMIT}
          Argon version: ${ARGON_VERSION}
          Argon commit: ${ARGON_COMMIT}
          Target: x86_64 Generic
          Firmware: EFI
          Kernel: 6.12.x
          Build time: ${DATE} Asia/Shanghai
          Firmware file: ${FILE}
          EOF

          echo "RELEASE_TAG=ImmortalWrt-25.12-x86_64-${DATE}" >> "$GITHUB_ENV"
          echo "RELEASE_FILE=$FILE" >> "$GITHUB_ENV"

          echo "============================================================"
          echo "发布文件列表"
          echo "============================================================"
          ls -lh release/

      # 22. 上传固件和诊断信息
      - name: 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-x86-64-${{ github.run_number }}
          path: |
            ./openwrt/release/*.img.gz
            ./openwrt/release/SHA256SUMS.txt
            ./openwrt/release/buildinfo.txt
          if-no-files-found: error
          retention-days: 14

      - name: 上传诊断信息
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-diagnostics-${{ github.run_number }}
          path: |
            ./openwrt/.config
            ./openwrt/.config.requested
            ./openwrt/defconfig.log
            ./openwrt/build.log
            ./openwrt/release/buildinfo.txt
          if-no-files-found: warn
          retention-days: 14

      # 23. 创建 GitHub Release
      - name: 创建 GitHub Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_TAG }}
          body: |
            ## ImmortalWrt 25.12 x86_64

            **固件：** `${{ env.RELEASE_FILE }}`
            **目标平台：** x86_64 Generic
            **固件格式：** EFI
            **编译时间：** 北京时间

            ### 主要组件

            - OpenClash
            - PassWall2
            - DAED
            - Lucky
            - RTP2HTTPD
            - Argon Theme
            - SQM / CAKE
            - WireGuard
            - OpenVPN

            ### 源码

            **ImmortalWrt：** `openwrt-25.12`
            **ImmortalWrt commit：** `${{ env.SOURCE_COMMIT }}`
            **Argon：** `master`
            **Argon commit：** `${{ env.ARGON_COMMIT }}`

            > 本 Release 为 GitHub Actions 自动编译发布。
          files: |
            ./openwrt/release/${{ env.RELEASE_FILE }}
            ./openwrt/release/SHA256SUMS.txt
            ./openwrt/release/buildinfo.txt
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 24. 删除旧的 workflow 运行记录
      #- name: 删除旧的 workflow 运行记录
      #  if: always()
      #  uses: Mattraks/delete-workflow-runs@v2
      #  continue-on-error: true
      #  with:
      #    token: ${{ github.token }}
      #    repository: ${{ github.repository }}
      #    retain_days: 20

      - name: 最终总结
        if: always()
        working-directory: ./openwrt
        run: |
          echo "################################################################"
          echo "# IMMORTALWRT 编译已完成"
          echo "################################################################"

          git log -1 --format='commit: %H'
          git log -1 --format='subject: %s'

          echo "ImmortalWrt commit: ${SOURCE_COMMIT:-unknown}"
          echo "Argon commit: ${ARGON_COMMIT:-unknown}"
          echo "Argon version: ${ARGON_VERSION:-unknown}"
          echo "Release: ${RELEASE_TAG:-not-created}"
          echo "Image: ${RELEASE_FILE:-not-created}"

          echo
          echo "CCache:"
          ccache -s || true

          echo "################################################################"

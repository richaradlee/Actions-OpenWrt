name: ImmortalWrt x86 V2

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    if: github.event.repository.owner.id == github.event.sender.id

    permissions:
      contents: write
      actions: write

    name: Build ${{ matrix.target }}

    strategy:
      fail-fast: false
      matrix:
        target: [25.12]

    steps:

      # ============================================================
      # 1. Checkout
      # ============================================================

      - name: Checkout
        uses: actions/checkout@v4


      # ============================================================
      # 2. Maximize build space
      # ============================================================

      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 4096
          temp-reserve-mb: 1024
          swap-size-mb: 8192
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'


      # ============================================================
      # 3. System preparation
      # ============================================================

      - name: Space cleanup & Timezone
        env:
          DEBIAN_FRONTEND: noninteractive
          TZ: Asia/Shanghai
        run: |
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /usr/local/share/powershell \
            /usr/share/swift \
            /usr/local/.ghcup \
            /usr/lib/jvm \
            /etc/mysql \
            /etc/php \
            || true

          sudo -E apt-get update

          sudo -E apt-get -y install \
            ack \
            antlr3 \
            asciidoc \
            autoconf \
            automake \
            autopoint \
            binutils \
            bison \
            build-essential \
            bzip2 \
            ccache \
            clang \
            cmake \
            cpio \
            curl \
            device-tree-compiler \
            flex \
            gawk \
            gcc-multilib \
            g++-multilib \
            gettext \
            genisoimage \
            git \
            gperf \
            haveged \
            help2man \
            intltool \
            libc6-dev-i386 \
            libelf-dev \
            libfuse-dev \
            libglib2.0-dev \
            libgmp3-dev \
            libltdl-dev \
            libmpc-dev \
            libmpfr-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libpython3-dev \
            libreadline-dev \
            libssl-dev \
            libtool \
            llvm \
            lrzsz \
            ninja-build \
            p7zip \
            p7zip-full \
            patch \
            pkgconf \
            python3 \
            python3-pyelftools \
            python3-setuptools \
            qemu-utils \
            rsync \
            scons \
            squashfs-tools \
            subversion \
            swig \
            texinfo \
            uglifyjs \
            upx-ucl \
            unzip \
            vim \
            wget \
            xmlto \
            xxd \
            zlib1g-dev \
            scdoc

          sudo -E timedatectl set-timezone "Asia/Shanghai"


      # ============================================================
      # 4. Build date
      # ============================================================

      - name: Get current date
        id: date
        env:
          TZ: Asia/Shanghai
        run: |
          echo "date=$(TZ=Asia/Shanghai date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV
          echo "date2=$(TZ=Asia/Shanghai date +'%m/%d %Y')" >> $GITHUB_ENV
          echo "date3=$(TZ=Asia/Shanghai date +'%m.%d')" >> $GITHUB_ENV


      # ============================================================
      # 5. Clone ImmortalWrt
      # ============================================================

      - name: Clone source code
        env:
          REPO_URL: https://github.com/immortalwrt/immortalwrt
          REPO_BRANCH: openwrt-25.12
        run: |
          git clone --depth 1 \
            "$REPO_URL" \
            -b "$REPO_BRANCH" \
            openwrt


      # ============================================================
      # 6. Update feeds
      #
      # small / kenzo:
      #   第三方代理插件及依赖
      #
      # lucky:
      #   Lucky
      #
      # rtp2httpd:
      #   RTP2HTTPD
      #
      # vmware:
      #   open-vm-tools
      # ============================================================

      - name: Update feeds
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p preflight

          echo "=================================================="
          echo " Adding third-party feeds"
          echo "=================================================="

          sed -i \
            '1i src-git kenzo https://github.com/kenzok8/openwrt-packages' \
            feeds.conf.default

          sed -i \
            '2i src-git small https://github.com/kenzok8/small' \
            feeds.conf.default

          sed -i \
            '3i src-git vmware https://github.com/vmware/open-vm-tools' \
            feeds.conf.default

          sed -i \
            '4i src-git rtp2httpd https://github.com/stackia/rtp2httpd.git' \
            feeds.conf.default

          sed -i \
            '5i src-git lucky https://github.com/gdy666/luci-app-lucky' \
            feeds.conf.default

          echo
          echo "===== feeds.conf.default ====="
          cat feeds.conf.default | tee preflight/feeds.conf.default


          echo
          echo "=================================================="
          echo " Updating feeds"
          echo "=================================================="

          ./scripts/feeds update -a \
            2>&1 | tee preflight/feeds-update.log


          # --------------------------------------------------------
          # Remove conflicting packages from ImmortalWrt feeds
          # --------------------------------------------------------

          echo
          echo "===== Removing conflicting packages ====="

          rm -rf feeds/luci/applications/luci-app-mosdns

          rm -rf \
            feeds/packages/net/alist \
            feeds/packages/net/adguardhome \
            feeds/packages/net/mosdns \
            feeds/packages/net/xray* \
            feeds/packages/net/v2ray* \
            feeds/packages/net/sing* \
            feeds/packages/net/smartdns

          rm -rf feeds/packages/utils/v2dat

          rm -rf feeds/packages/lang/golang


          # --------------------------------------------------------
          # Use kenzok8 golang 1.26
          # --------------------------------------------------------

          echo
          echo "===== Installing kenzok8/golang 1.26 ====="

          git clone \
            --depth 1 \
            -b 1.26 \
            https://github.com/kenzok8/golang.git \
            feeds/packages/lang/golang


          # --------------------------------------------------------
          # Install all feeds
          # --------------------------------------------------------

          echo
          echo "===== Installing feeds ====="

          ./scripts/feeds install -a \
            2>&1 | tee preflight/feeds-install.log


          # --------------------------------------------------------
          # Known problematic packages
          # --------------------------------------------------------

          echo
          echo "===== Removing known problematic packages ====="

          # luci-app-fchomo:
          # small 中存在递归依赖问题
          rm -rf package/feeds/small/luci-app-fchomo

          # trojan-plus:
          # 当前版本存在 boost-system 等无效依赖问题
          rm -rf package/feeds/small/trojan-plus


          echo
          echo "===== Feed status ====="

          ./scripts/feeds list -s \
            | tee preflight/feeds-status.txt


      # ============================================================
      # 7. PRE-FLIGHT - Feed / Package validation
      #
      # 这一阶段只检查，不进行完整固件编译。
      # 如果这里失败，整个 workflow 会立即停止。
      # ============================================================

      - name: PRE-FLIGHT - Feed and package validation
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          echo "=================================================="
          echo " PRE-FLIGHT - Feed / Package validation"
          echo "=================================================="

          mkdir -p preflight


          # --------------------------------------------------------
          # Helper
          # --------------------------------------------------------

          check_pkg() {
            local name="$1"
            local path="$2"

            if [ -e "$path" ]; then
              echo "[ OK ] $name -> $path"
            else
              echo "[FAIL] $name -> $path"
              return 1
            fi
          }


          # --------------------------------------------------------
          # Check selected third-party packages
          # --------------------------------------------------------

          echo
          echo "===== Selected third-party packages ====="

          check_pkg \
            "luci-app-passwall2" \
            "package/feeds/small/luci-app-passwall2"

          check_pkg \
            "daed" \
            "package/feeds/small/daed"

          check_pkg \
            "luci-app-daed" \
            "package/feeds/small/luci-app-daed"

          check_pkg \
            "luci-app-rtp2httpd" \
            "package/feeds/rtp2httpd/luci-app-rtp2httpd"

          check_pkg \
            "luci-app-lucky" \
            "package/feeds/lucky/luci-app-lucky"

          check_pkg \
            "luci-app-openclash" \
            "feeds/luci/applications/luci-app-openclash"


          # --------------------------------------------------------
          # Verify removed packages
          # --------------------------------------------------------

          echo
          echo "===== Removed problematic packages ====="

          if [ -e package/feeds/small/luci-app-fchomo ]; then
            echo "[FAIL] luci-app-fchomo still exists"
            exit 1
          else
            echo "[ OK ] luci-app-fchomo removed"
          fi

          if [ -e package/feeds/small/trojan-plus ]; then
            echo "[FAIL] trojan-plus still exists"
            exit 1
          else
            echo "[ OK ] trojan-plus removed"
          fi


          # --------------------------------------------------------
          # Check package versions
          # --------------------------------------------------------

          echo
          echo "=================================================="
          echo " Third-party package versions"
          echo "=================================================="


          echo
          echo "===== DAED ====="

          grep -R \
            --line-number \
            -E 'PKG_VERSION|PKG_RELEASE' \
            package/feeds/small/daed \
            2>/dev/null \
            | tee preflight/daed-version.txt \
            || true


          echo
          echo "===== RTP2HTTPD ====="

          grep -R \
            --line-number \
            -E 'PKG_VERSION|PKG_RELEASE' \
            package/feeds/rtp2httpd \
            2>/dev/null \
            | tee preflight/rtp2httpd-version.txt \
            || true


          echo
          echo "===== PASSWALL2 ====="

          grep -R \
            --line-number \
            -E 'PKG_VERSION|PKG_RELEASE' \
            package/feeds/small/luci-app-passwall2 \
            2>/dev/null \
            | tee preflight/passwall2-version.txt \
            || true


          echo
          echo "===== LUCKY ====="

          grep -R \
            --line-number \
            -E 'PKG_VERSION|PKG_RELEASE' \
            package/feeds/lucky \
            2>/dev/null \
            | tee preflight/lucky-version.txt \
            || true


          # --------------------------------------------------------
          # Search invalid dependencies
          # --------------------------------------------------------

          echo
          echo "=================================================="
          echo " Dependency scan"
          echo "=================================================="

          echo
          echo "===== boost dependencies ====="

          grep -R \
            --line-number \
            -E 'boost-system|boost-program_options|boost-date_time' \
            package feeds \
            2>/dev/null \
            | tee preflight/boost-dependencies.txt \
            || true


          echo
          echo "===== fchomo references ====="

          grep -R \
            --line-number \
            "luci-app-fchomo" \
            package feeds \
            2>/dev/null \
            | tee preflight/fchomo-references.txt \
            || true


          echo
          echo "=================================================="
          echo " Package validation completed"
          echo "=================================================="


      # ============================================================
      # 8. Get new themes
      # ============================================================

      - name: Get new themes
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          rm -rf package/feeds/luci/luci-theme-argon
          rm -rf package/feeds/luci/luci-app-argon-config
          rm -rf package/feeds/luci/luci-theme-infinityfreedom
          rm -rf package/feeds/luci/luci/luci-theme-design
          rm -rf package/feeds/luci/luci-theme-opentopd
          rm -rf package/feeds/luci/luci-theme-neobird

          git clone \
            -b master \
            https://github.com/jerrykuku/luci-theme-argon.git \
            package/feeds/luci/luci-theme-argon

          git clone \
            -b master \
            https://github.com/jerrykuku/luci-app-argon-config.git \
            package/feeds/luci/luci-app-argon-config


      # ============================================================
      # 9. Generate configuration
      # ============================================================

      - name: Custom configure file
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p preflight

          rm -f ./.config*
          touch ./.config


          # --------------------------------------------------------
          # System timezone
          # --------------------------------------------------------

          mkdir -p package/base-files/files/etc/config

          if [ -f package/base-files/files/etc/config/system ]; then

            sed -i \
              "s/^[[:space:]]*option timezone.*/	option timezone 'CST-8'/" \
              package/base-files/files/etc/config/system

            sed -i \
              "s/^[[:space:]]*option zonename.*/	option zonename 'Asia\/Shanghai'/" \
              package/base-files/files/etc/config/system

          else

            cat > package/base-files/files/etc/config/system <<'EOF'
          config system
                  option hostname 'OpenWrt'
                  option timezone 'CST-8'
                  option zonename 'Asia/Shanghai'
          EOF

          fi


          # ========================================================
          # Target
          # ========================================================

          cat >> .config <<EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          EOF


          # ========================================================
          # Image size
          # ========================================================

          cat >> .config <<EOF
          CONFIG_TARGET_KERNEL_PARTSIZE=128
          CONFIG_TARGET_ROOTFS_PARTSIZE=640
          CONFIG_TARGET_IMAGES_GZIP=y
          CONFIG_EFI_IMAGES=y
          EOF


          # ========================================================
          # IPv6
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_odhcp6c=y
          CONFIG_PACKAGE_odhcpd-ipv6only=y
          CONFIG_PACKAGE_luci-proto-ipv6=y
          CONFIG_PACKAGE_ip6tables=y
          CONFIG_PACKAGE_kmod-nft-nat6=y
          EOF


          # ========================================================
          # Bonding
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_kmod-bonding=y
          CONFIG_PACKAGE_proto-bonding=y
          CONFIG_PACKAGE_luci-proto-bonding=y
          EOF


          # ========================================================
          # LuCI
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-app-diskman=y
          CONFIG_PACKAGE_luci-app-lucky=y
          CONFIG_PACKAGE_luci-app-filebrowser=n
          CONFIG_PACKAGE_luci-app-irqbalance=y
          CONFIG_PACKAGE_luci-app-taskplan=n
          CONFIG_PACKAGE_luci-app-package-manager=y
          CONFIG_PACKAGE_luci-app-sqm=y
          CONFIG_PACKAGE_sqm-scripts=y
          CONFIG_PACKAGE_kmod-sched-cake=y
          CONFIG_PACKAGE_kmod-ifb=y
          CONFIG_PACKAGE_tc-full=y
          EOF


          # ========================================================
          # IPTV
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_luci-app-rtp2httpd=y
          EOF


          # ========================================================
          # Proxy
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_luci-app-ssr-plus=n
          CONFIG_PACKAGE_luci-app-passwall=n
          CONFIG_PACKAGE_luci-app-passwall2=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-mihomo=n
          CONFIG_PACKAGE_luci-app-homeproxy=n
          EOF


          # ========================================================
          # DAED
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_daed=y
          CONFIG_PACKAGE_luci-app-daed=y
          CONFIG_PACKAGE_kmod-nft-tproxy=y
          CONFIG_PACKAGE_kmod-nft-socket=y
          CONFIG_PACKAGE_kmod-nft-compat=y
          CONFIG_PACKAGE_nftables=y
          CONFIG_PACKAGE_ip-full=y
          EOF


          # ========================================================
          # VPN
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_luci-app-softethervpn=n
          CONFIG_PACKAGE_luci-app-openvpn=y
          CONFIG_PACKAGE_luci-app-openvpn-server=y
          CONFIG_PACKAGE_openvpn-easy-rsa=y
          CONFIG_PACKAGE_netbird=n
          CONFIG_PACKAGE_wireguard-tools=y
          CONFIG_PACKAGE_kmod-wireguard=y
          CONFIG_PACKAGE_luci-app-wireguard=y
          CONFIG_PACKAGE_luci-proto-wireguard=y
          EOF


          # ========================================================
          # Theme
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_luci-theme-argon=y
          EOF


          # ========================================================
          # Common packages
          # ========================================================

          cat >> .config <<EOF
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_screen=y
          CONFIG_PACKAGE_tree=y
          CONFIG_PACKAGE_vim-fuller=n
          CONFIG_PACKAGE_wget=y
          CONFIG_PACKAGE_acme=y
          CONFIG_PACKAGE_acme-dnsapi=y
          CONFIG_PACKAGE_haproxy=y
          CONFIG_PACKAGE_socat=y
          CONFIG_SOCAT_SSL=y
          CONFIG_SING_BOX_WITH_ECH=y
          CONFIG_PACKAGE_sing-box=y
          CONFIG_PACKAGE_chinadns-ng=y
          CONFIG_PACKAGE_open-vm-tools=y
          CONFIG_PACKAGE_iperf3=y
          CONFIG_PACKAGE_iftop=y
          CONFIG_PACKAGE_tcpdump=y
          CONFIG_PACKAGE_pv=y
          CONFIG_PACKAGE_bind-host=y
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_dns2socks=y
          CONFIG_PACKAGE_tcping=y
          CONFIG_PACKAGE_geoview=y
          CONFIG_PACKAGE_irqbalance=y
          EOF


          # --------------------------------------------------------
          # Clean whitespace
          # --------------------------------------------------------

          sed -i 's/^[ \t]*//g' ./.config


          # ========================================================
          # Kconfig resolution
          # ========================================================

          echo "=================================================="
          echo " Running make defconfig"
          echo "=================================================="

          make defconfig \
            2>&1 | tee preflight/defconfig.log


          # ========================================================
          # Selected packages
          # ========================================================

          echo
          echo "===== Selected packages ====="

          grep -E \
            '^CONFIG_PACKAGE_(luci-app-passwall2|luci-app-openclash|daed|luci-app-daed|luci-app-rtp2httpd|luci-app-lucky)=' \
            .config \
            | tee preflight/selected-packages.txt \
            || true


          echo
          echo "===== Disabled problematic packages ====="

          grep -E \
            '^CONFIG_PACKAGE_(luci-app-fchomo|trojan-plus)=' \
            .config \
            | tee preflight/disabled-packages.txt \
            || true


          # ========================================================
          # LLVM / Clang environment
          # ========================================================

          echo
          echo "=================================================="
          echo " LLVM / Clang environment"
          echo "=================================================="

          {
            echo "PATH=$PATH"
            echo "CC=$CC"
            echo "HOSTCC=$HOSTCC"
            echo "CLANG=$CLANG"
            echo "LLVM=$LLVM"

            echo
            echo "--- clang ---"
            which clang || true
            clang --version || true

            echo
            echo "--- llvm-config ---"
            which llvm-config || true
            llvm-config --version || true

          } | tee preflight/llvm.txt


          # ========================================================
          # Invalid clang search
          # ========================================================

          echo
          echo "===== Search invalid clang ====="

          grep -R \
            --line-number \
            "/invalid/clang" \
            .config \
            include \
            package \
            feeds \
            2>/dev/null \
            | head -100 \
            | tee preflight/invalid-clang.txt \
            || true


          # ========================================================
          # LLVM config
          # ========================================================

          echo
          echo "===== LLVM config ====="

          grep -E \
            'CONFIG_(LLVM|BPF|DEBUG_INFO_BTF)' \
            .config \
            | tee preflight/llvm-config.txt \
            || true


          echo
          echo "=================================================="
          echo " PRE-FLIGHT CONFIGURATION PASSED"
          echo "=================================================="


      # ============================================================
      # 10. Preflight package metadata
      #
      # 注意：
      # 不执行完整 package compile。
      # 这里只做非常轻量的 metadata/index 检查。
      # ============================================================

      - name: PRE-FLIGHT - Package metadata check
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          echo "=================================================="
          echo " PRE-FLIGHT - Package metadata check"
          echo "=================================================="

          mkdir -p preflight

          echo
          echo "===== Check selected Makefiles ====="

          files=(
            "package/feeds/small/luci-app-passwall2/Makefile"
            "package/feeds/small/daed/Makefile"
            "package/feeds/small/luci-app-daed/Makefile"
            "package/feeds/rtp2httpd/luci-app-rtp2httpd/Makefile"
          )

          for f in "${files[@]}"; do
            if [ -f "$f" ]; then
              echo "[ OK ] $f"
            else
              echo "[FAIL] Missing $f"
              exit 1
            fi
          done


          echo
          echo "===== Package metadata ====="

          {
            echo "===== PassWall2 ====="
            sed -n '1,180p' \
              package/feeds/small/luci-app-passwall2/Makefile

            echo
            echo "===== DAED ====="
            sed -n '1,180p' \
              package/feeds/small/daed/Makefile

            echo
            echo "===== luci-app-daed ====="
            sed -n '1,180p' \
              package/feeds/small/luci-app-daed/Makefile

            echo
            echo "===== RTP2HTTPD ====="
            sed -n '1,180p' \
              package/feeds/rtp2httpd/luci-app-rtp2httpd/Makefile

          } | tee preflight/package-makefiles.txt


          echo
          echo "=================================================="
          echo " PACKAGE METADATA CHECK PASSED"
          echo "=================================================="


      # ============================================================
      # 11. Upload PRE-FLIGHT logs
      #
      # 只要前面的步骤失败，也能拿到诊断信息。
      # ============================================================

      - name: Upload preflight logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-preflight-${{ env.date }}
          path: ./openwrt/preflight/
          if-no-files-found: warn
          retention-days: 7


      # ============================================================
      # 12. Full firmware compilation
      #
      # 只有所有 PRE-FLIGHT 检查通过后才会执行。
      # ============================================================

      - name: Compile the firmware
        working-directory: ./openwrt
        shell: bash
        run: |
          set -o pipefail

          echo "=================================================="
          echo " PRE-FLIGHT PASSED"
          echo " Starting full firmware compilation"
          echo "=================================================="

          make download -j8

          make -j1 V=s 2>&1 | tee build.log


      # ============================================================
      # 13. Compress build log
      # ============================================================

      - name: Compress build log
        if: always()
        working-directory: ./openwrt
        shell: bash
        run: |
          if [ -f build.log ]; then
            gzip -9 -f build.log
          fi


      # ============================================================
      # 14. Upload build log
      # ============================================================

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-build-log-${{ env.date }}
          path: ./openwrt/build.log.gz
          if-no-files-found: warn
          retention-days: 7


      # ============================================================
      # 15. Get kernel version
      # ============================================================

      - name: Get kernel version
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          KERNEL_MAKEFILE=$(find build_dir \
            -path '*/linux-x86_64/linux-*/Makefile' \
            -type f \
            | head -1)

          if [ -z "$KERNEL_MAKEFILE" ]; then
            echo "ERROR: Linux kernel Makefile not found."
            exit 1
          fi

          KERNEL_DIR=$(dirname "$KERNEL_MAKEFILE")

          VERSION=$(grep '^VERSION = ' "$KERNEL_MAKEFILE" | awk '{print $3}')
          PATCHLEVEL=$(grep '^PATCHLEVEL = ' "$KERNEL_MAKEFILE" | awk '{print $3}')
          SUBLEVEL=$(grep '^SUBLEVEL = ' "$KERNEL_MAKEFILE" | awk '{print $3}')

          KERNEL_VERSION="${VERSION}.${PATCHLEVEL}.${SUBLEVEL}"

          echo "Kernel source:"
          echo "$KERNEL_DIR"

          echo "Kernel version: $KERNEL_VERSION"

          echo "KERNEL_VERSION=$KERNEL_VERSION" >> "$GITHUB_ENV"


      # ============================================================
      # 16. Prepare artifacts
      # ============================================================

      - name: Prepare artifact
        working-directory: ./openwrt
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p artifact/firmware
          mkdir -p artifact/package
          mkdir -p artifact/buildinfo

          find ./bin/targets/ \
            -type f \
            -name "openwrt-x86-64-generic-kernel.bin" \
            -delete

          find ./bin/targets/ \
            -type f \
            -name "openwrt-x86-64-generic-squashfs-rootfs.img.gz" \
            -delete

          find ./bin/targets/ \
            -type d \
            -name "packages" \
            -prune \
            -exec rm -rf {} +

          cp \
            $(find ./bin/targets/ \
              -type f \
              -name "*combined-efi.img.gz") \
            ./artifact/firmware/

          cp \
            $(find ./bin/packages/ \
              -type f \
              -name "*.ipk") \
            ./artifact/package/

          cp \
            $(find ./bin/targets/ \
              -type f \
              \( \
                -name "*.buildinfo" \
                -o \
                -name "*.manifest" \
              \)) \
            ./artifact/buildinfo/


      # ============================================================
      # 17. Deliver buildinfo
      # ============================================================

      - name: Deliver buildinfo
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.date }}.ImmortalWRT.x86.buildinfo.25.12
          path: ./openwrt/artifact/buildinfo/
          retention-days: 30


      # ============================================================
      # 18. Deliver packages
      # ============================================================

      - name: Deliver package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.date }}.ImmortalWRT.x86.package.25.12
          path: ./openwrt/artifact/package/
          retention-days: 30


      # ============================================================
      # 19. Deliver firmware
      # ============================================================

      - name: Deliver firmware
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.date }}.ImmortalWRT.x86.firmware.25.12
          path: ./openwrt/artifact/firmware/
          retention-days: 30


      # ============================================================
      # 20. GitHub Release
      # ============================================================

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: OpenWrt-${{ env.date }}

          name: OpenWrt 25.12 x86_64 - ${{ env.date }}

          body: |
            ## OpenWrt 25.12 x86_64

            **固件内核：** ${{ env.KERNEL_VERSION }}

            **编译时间：** ${{ env.date }}（北京时间）

            **目标平台：** x86_64 Generic

            **固件格式：** EFI

            **主要组件：**
            - OpenClash
            - PassWall2
            - DAED
            - SQM / CAKE
            - WireGuard
            - OpenVPN
            - Lucky
            - RTP2HTTPD
            - Argon Theme

            **源码：** ImmortalWrt openwrt-25.12

            **软件库：** kenzok8/small

            > 本 Release 为 GitHub Actions 自动编译发布。

          files: |
            ./openwrt/artifact/firmware/*
            ./openwrt/artifact/buildinfo/*

          draft: false
          prerelease: false
          generate_release_notes: false

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


      # ============================================================
      # 21. Delete old workflow runs
      # ============================================================

      - name: Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        continue-on-error: true
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 20
          keep_minimum_runs: 3

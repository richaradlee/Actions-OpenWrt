name: ImmortalWrt V3 Test 05

on:
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest

    if: github.event.repository.owner.id == github.event.sender.id

    permissions:
      contents: read
      actions: write

    name: Build Test ${{ matrix.target }}

    strategy:
      fail-fast: false
      matrix:
        target: [25.12]

    steps:

      # ============================================================
      # 1. Checkout
      # ============================================================

      - name: Checkout
        uses: actions/checkout@v4


      # ============================================================
      # 2. Maximize build space
      # ============================================================

      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 4096
          temp-reserve-mb: 1024
          swap-size-mb: 8192
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'


      # ============================================================
      # 3. Install build dependencies
      # ============================================================

      - name: Install build dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
          TZ: Asia/Shanghai

        run: |
          set -e

          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /usr/local/share/powershell \
            /usr/share/swift \
            /usr/local/.ghcup \
            /usr/lib/jvm \
            /etc/mysql \
            /etc/php || true

          sudo -E apt-get update

          sudo -E apt-get install -y \
            ack \
            asciidoc \
            autoconf \
            automake \
            autopoint \
            binutils \
            bison \
            build-essential \
            bzip2 \
            ccache \
            clang \
            cmake \
            cpio \
            curl \
            device-tree-compiler \
            flex \
            gawk \
            gettext \
            git \
            gperf \
            help2man \
            intltool \
            libelf-dev \
            libglib2.0-dev \
            libgmp3-dev \
            libltdl-dev \
            libmpc-dev \
            libmpfr-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libpython3-dev \
            libreadline-dev \
            libssl-dev \
            libtool \
            llvm \
            make \
            ninja-build \
            patch \
            pkgconf \
            python3 \
            python3-pyelftools \
            python3-setuptools \
            rsync \
            scons \
            squashfs-tools \
            subversion \
            swig \
            texinfo \
            uglifyjs \
            unzip \
            wget \
            xmlto \
            xxd \
            zlib1g-dev \
            scdoc

          sudo -E timedatectl set-timezone "Asia/Shanghai"


      # ============================================================
      # 4. Clone ImmortalWrt
      # ============================================================

      - name: Clone ImmortalWrt source
        env:
          REPO_URL: https://github.com/immortalwrt/immortalwrt
          REPO_BRANCH: openwrt-25.12

        run: |
          set -e

          git clone \
            --depth 1 \
            "$REPO_URL" \
            -b "$REPO_BRANCH" \
            openwrt

          cd openwrt

          echo
          echo "============================================================"
          echo "IMMORTALWRT SOURCE"
          echo "============================================================"

          git branch --show-current
          git log -1 --format='commit: %H'
          git log -1 --format='subject: %s'

          echo
          echo "============================================================"


      # ============================================================
      # 5. Configure feeds
      # ============================================================

      - name: Configure upstream feeds
        working-directory: ./openwrt

        run: |
          set -e

          sed -i \
            '1i src-git rtp2httpd https://github.com/stackia/rtp2httpd.git' \
            feeds.conf.default

          echo
          echo "============================================================"
          echo "FEEDS CONFIGURATION"
          echo "============================================================"

          cat feeds.conf.default


      # ============================================================
      # 6. Update feeds
      # ============================================================

      - name: Update feeds
        working-directory: ./openwrt

        run: |
          set -e

          ./scripts/feeds update -a
          ./scripts/feeds install -a


      # ============================================================
      # 7. Install third-party packages
      # ============================================================

      - name: Install third-party packages
        working-directory: ./openwrt

        run: |
          set -e

          echo
          echo "============================================================"
          echo "INSTALL THIRD-PARTY PACKAGES"
          echo "============================================================"


          # ----------------------------------------------------------
          # OpenClash
          # ----------------------------------------------------------

          rm -rf package/feeds/luci/luci-app-openclash
          rm -rf package/openclash

          git clone \
            --depth 1 \
            https://github.com/vernesong/OpenClash.git \
            package/openclash


          # ----------------------------------------------------------
          # PassWall2
          # ----------------------------------------------------------

          rm -rf package/feeds/luci/luci-app-passwall2
          rm -rf package/passwall2

          git clone \
            --depth 1 \
            https://github.com/Openwrt-Passwall/openwrt-passwall2.git \
            package/passwall2


          # ----------------------------------------------------------
          # DAED
          # ----------------------------------------------------------

          rm -rf package/dae
          rm -rf package/feeds/base/dae

          git clone \
            --depth 1 \
            https://github.com/QiuSimons/luci-app-daed.git \
            package/dae


          # ----------------------------------------------------------
          # Lucky
          # ----------------------------------------------------------

          rm -rf package/feeds/lucky/luci-app-lucky
          rm -rf package/lucky

          git clone \
            --depth 1 \
            https://github.com/gdy666/luci-app-lucky.git \
            package/lucky


          # ----------------------------------------------------------
          # Argon
          # ----------------------------------------------------------

          rm -rf package/feeds/luci/luci-theme-argon
          rm -rf package/feeds/luci/luci-app-argon-config

          rm -rf package/argon
          rm -rf package/argon-config

          git clone \
            --depth 1 \
            https://github.com/jerrykuku/luci-theme-argon.git \
            package/argon

          git clone \
            --depth 1 \
            https://github.com/jerrykuku/luci-app-argon-config.git \
            package/argon-config


          echo
          echo "============================================================"
          echo "THIRD-PARTY PACKAGES INSTALLED"
          echo "============================================================"


      # ============================================================
      # 8. Refresh feed packages
      # ============================================================

      - name: Refresh feed packages
        working-directory: ./openwrt

        run: |
          set -e

          ./scripts/feeds install -a


      # ============================================================
      # 9. Generate V3 configuration
      # ============================================================

      - name: Generate V3 configuration
        working-directory: ./openwrt

        shell: bash

        run: |
          set -e

          echo "============================================================"
          echo "GENERATE IMMORTALWRT V3 CONFIGURATION"
          echo "============================================================"

          rm -f .config .config.old

          touch .config


          # ==========================================================
          # Target
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y

          CONFIG_TARGET_KERNEL_PARTSIZE=128
          CONFIG_TARGET_ROOTFS_PARTSIZE=640

          CONFIG_TARGET_IMAGES_GZIP=y
          CONFIG_EFI_IMAGES=y
          EOF


          # ==========================================================
          # IPv6
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_odhcp6c=y
          CONFIG_PACKAGE_odhcpd-ipv6only=y
          CONFIG_PACKAGE_luci-proto-ipv6=y
          CONFIG_PACKAGE_ip6tables=y
          CONFIG_PACKAGE_kmod-nft-nat6=y
          EOF


          # ==========================================================
          # Bonding
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_kmod-bonding=y
          CONFIG_PACKAGE_proto-bonding=y
          CONFIG_PACKAGE_luci-proto-bonding=y
          EOF


          # ==========================================================
          # LuCI
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-app-diskman=y
          CONFIG_PACKAGE_luci-app-irqbalance=y
          CONFIG_PACKAGE_luci-app-package-manager=y

          CONFIG_PACKAGE_luci-app-filebrowser=n
          CONFIG_PACKAGE_luci-app-taskplan=n
          EOF


          # ==========================================================
          # SQM / CAKE
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-app-sqm=y
          CONFIG_PACKAGE_sqm-scripts=y
          CONFIG_PACKAGE_kmod-sched-cake=y
          CONFIG_PACKAGE_kmod-ifb=y
          CONFIG_PACKAGE_tc-full=y
          EOF


          # ==========================================================
          # IPTV
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-app-rtp2httpd=y
          EOF


          # ==========================================================
          # Proxy
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-app-ssr-plus=n
          CONFIG_PACKAGE_luci-app-passwall=n
          CONFIG_PACKAGE_luci-app-passwall2=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-mihomo=n
          CONFIG_PACKAGE_luci-app-homeproxy=n
          EOF


          # ==========================================================
          # DAED / eBPF / BTF
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_DEVEL=y

          CONFIG_KERNEL_DEBUG_INFO=y
          CONFIG_KERNEL_DEBUG_INFO_REDUCED=n
          CONFIG_KERNEL_DEBUG_INFO_BTF=y

          CONFIG_KERNEL_CGROUPS=y
          CONFIG_KERNEL_CGROUP_BPF=y
          CONFIG_KERNEL_BPF_EVENTS=y

          CONFIG_BPF_TOOLCHAIN_HOST=y
          CONFIG_KERNEL_XDP_SOCKETS=y

          CONFIG_PACKAGE_kmod-xdp-sockets-diag=y

          CONFIG_PACKAGE_daed=y
          CONFIG_PACKAGE_luci-app-daed=y

          CONFIG_PACKAGE_kmod-nft-tproxy=y
          CONFIG_PACKAGE_kmod-nft-socket=y
          CONFIG_PACKAGE_kmod-nft-compat=y
          CONFIG_PACKAGE_nftables=y
          CONFIG_PACKAGE_ip-full=y
          EOF


          # ==========================================================
          # VPN
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-app-softethervpn=n

          CONFIG_PACKAGE_luci-app-openvpn=y
          CONFIG_PACKAGE_luci-app-openvpn-server=y
          CONFIG_PACKAGE_openvpn-easy-rsa=y

          CONFIG_PACKAGE_netbird=n

          CONFIG_PACKAGE_wireguard-tools=y
          CONFIG_PACKAGE_kmod-wireguard=y
          CONFIG_PACKAGE_luci-app-wireguard=y
          CONFIG_PACKAGE_luci-proto-wireguard=y
          EOF


          # ==========================================================
          # Lucky
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-app-lucky=y
          EOF


          # ==========================================================
          # Argon
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_PACKAGE_luci-app-argon-config=y
          EOF


          # ==========================================================
          # Common packages
          # ==========================================================

          cat >> .config <<'EOF'
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_screen=y
          CONFIG_PACKAGE_tree=y

          CONFIG_PACKAGE_vim-fuller=n
          CONFIG_PACKAGE_wget=y

          CONFIG_PACKAGE_acme=y
          CONFIG_PACKAGE_acme-dnsapi=y

          CONFIG_PACKAGE_haproxy=y
          CONFIG_PACKAGE_socat=y
          CONFIG_SOCAT_SSL=y

          CONFIG_SING_BOX_WITH_ECH=y
          CONFIG_PACKAGE_sing-box=y

          CONFIG_PACKAGE_chinadns-ng=y

          CONFIG_PACKAGE_open-vm-tools=y

          CONFIG_PACKAGE_iperf3=y
          CONFIG_PACKAGE_iftop=y
          CONFIG_PACKAGE_tcpdump=y
          CONFIG_PACKAGE_pv=y
          CONFIG_PACKAGE_bind-host=y
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_dns2socks=y
          CONFIG_PACKAGE_tcping=y
          CONFIG_PACKAGE_geoview=y
          CONFIG_PACKAGE_irqbalance=y
          EOF


          # ==========================================================
          # Save requested configuration
          # ==========================================================

          cp .config .config.requested


          # ==========================================================
          # Run defconfig
          # ==========================================================

          echo
          echo "============================================================"
          echo "RUN MAKE DECONFIG"
          echo "============================================================"

          make defconfig


          # ==========================================================
          # Save final configuration
          # ==========================================================

          cp .config .config.final


          echo
          echo "============================================================"
          echo "FINAL PACKAGE SELECTION"
          echo "============================================================"

          grep -E \
            '^CONFIG_PACKAGE_(luci-app-openclash|luci-app-passwall2|daed|luci-app-daed|luci-app-lucky|luci-app-rtp2httpd|luci-theme-argon|luci-app-argon-config|sing-box|kmod-wireguard|wireguard-tools|luci-app-wireguard|luci-app-sqm|sqm-scripts|kmod-sched-cake|kmod-ifb|tc-full)=' \
            .config \
            || true


      # ============================================================
      # 10. Final pre-build configuration check
      # ============================================================

      - name: Final pre-build configuration check
        working-directory: ./openwrt

        shell: bash

        run: |
          set -e

          echo "============================================================"
          echo "FINAL PRE-BUILD CONFIGURATION CHECK"
          echo "============================================================"

          FAILED=0

          check_y() {
            SYMBOL="$1"

            VALUE=$(grep "^${SYMBOL}=" .config 2>/dev/null \
              | cut -d= -f2 || true)

            echo "$SYMBOL = ${VALUE:-<NOT SET>}"

            if [ "$VALUE" != "y" ]; then
              echo "ERROR: $SYMBOL is not enabled."
              FAILED=1
            fi
          }

          check_n() {
            SYMBOL="$1"

            VALUE=$(grep "^${SYMBOL}=" .config 2>/dev/null \
              | cut -d= -f2 || true)

            echo "$SYMBOL = ${VALUE:-<NOT SET>}"

            if [ "$VALUE" != "n" ]; then
              echo "ERROR: $SYMBOL is not explicitly disabled."
              FAILED=1
            fi
          }


          echo
          echo "===== Required packages ====="

          check_y CONFIG_PACKAGE_luci
          check_y CONFIG_PACKAGE_luci-app-openclash
          check_y CONFIG_PACKAGE_luci-app-passwall2
          check_y CONFIG_PACKAGE_luci-app-daed
          check_y CONFIG_PACKAGE_daed
          check_y CONFIG_PACKAGE_luci-app-lucky
          check_y CONFIG_PACKAGE_luci-app-rtp2httpd
          check_y CONFIG_PACKAGE_luci-theme-argon
          check_y CONFIG_PACKAGE_luci-app-argon-config


          echo
          echo "===== VPN ====="

          check_y CONFIG_PACKAGE_kmod-wireguard
          check_y CONFIG_PACKAGE_wireguard-tools
          check_y CONFIG_PACKAGE_luci-app-wireguard
          check_y CONFIG_PACKAGE_luci-proto-wireguard


          echo
          echo "===== SQM ====="

          check_y CONFIG_PACKAGE_luci-app-sqm
          check_y CONFIG_PACKAGE_sqm-scripts
          check_y CONFIG_PACKAGE_kmod-sched-cake
          check_y CONFIG_PACKAGE_kmod-ifb
          check_y CONFIG_PACKAGE_tc-full


          echo
          echo "===== Disabled proxy frontends ====="

          check_n CONFIG_PACKAGE_luci-app-ssr-plus
          check_n CONFIG_PACKAGE_luci-app-passwall
          check_n CONFIG_PACKAGE_luci-app-mihomo
          check_n CONFIG_PACKAGE_luci-app-homeproxy
          check_n CONFIG_PACKAGE_netbird


          echo
          echo "===== Final result ====="

          if [ "$FAILED" -ne 0 ]; then
            echo
            echo "============================================================"
            echo "PRE-BUILD CONFIGURATION CHECK FAILED"
            echo "============================================================"
            exit 1
          fi

          echo
          echo "============================================================"
          echo "PRE-BUILD CONFIGURATION CHECK PASSED"
          echo "============================================================"


      # ============================================================
      # 11. Download sources
      # ============================================================

      - name: Download sources
        working-directory: ./openwrt

        shell: bash

        run: |
          set -o pipefail

          echo
          echo "============================================================"
          echo "DOWNLOAD SOURCE 
